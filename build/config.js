// Generated by CoffeeScript 1.9.3
var config, exports, fs, readOpts, tag;

tag = "config:%s =>";

fs = require("fs");

readOpts = {
  encoding: "utf8"
};

config = function() {
  this.programRoot = "/home/derek/.daytimer/";
  this.configFile = this.programRoot + ".daytimerconf.json";
  this.questionsDir = this.programRoot + "questions/";
  this.contents = JSON.parse(fs.readFileSync(this.configFile, readOpts));
  this.getQuestions = function() {
    var json, target, x;
    x = fs.readFileSync(this.configFile, readOpts);
    json = JSON.parse(x);
    target = json.questions;
    return target;
  };
  this.writeConfig = function(data) {
    var json;
    json = JSON.stringify(data, null, "\t");
    fs.writeFileSync(this.configFile, json);
    return console.log(tag, "config:write", "data written");
  };
  this.writeQuestion = function(data) {
    var json;
    json = JSON.stringify(data, null, "\t");
    return fs.writeFile(data.filename, json, function(err) {
      if (err) {
        throw err;
      }
      return console.log(tag, "writeQuestion:written", "question written successfully");
    });
  };
  return this;
};

exports = module.exports = config;
